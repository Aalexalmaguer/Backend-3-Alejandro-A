# Dependencias (se instalan dentro de la imagen)
node_modules
npm-debug.log*

# Variables de entorno y secretos (se pasan al contenedor por --env-file / -e)
.env
.env.local
.env.*.local

# Control de versiones
.git
.gitignore

# Archivos generados por la app (no deben ir en la imagen)
logs
uploads
uploads-test

# Testing y cobertura
test
coverage
.nyc_output
.mocharc.json

# Docker
Dockerfile
.dockerignore

# Sistema operativo / editor / temporales
.DS_Store
.vscode
.idea
*.tmp
*.log
